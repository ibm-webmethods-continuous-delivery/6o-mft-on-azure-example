# Azure DevOps Elastic Pool Configuration Exception

## Overview

As the SDK based Terraform provider for Azure DevOps has limited support for configuring elastic pools with VMSS integration, as an exception, configure one manually and change the name for the agent pool in the project variables.
